PROJECT 9 README

The Rental Safety application can be run by either running the RentalSafety jar file or
compiling all files in the rentalSafety package and running the DrawGUIInterface java file.

All tables must exist and be populated for the application to run as intended. All necessary
tables and corrosponding data can be created with the SchemaCreation.sql file.